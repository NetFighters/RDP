name: GetScreen Windows 10 

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Downloading & Setting Up
        run: |
         echo "EMAIL_SECRET=Your GetScreen Mail" > secrets.txt
         Invoke-WebRequest -Uri "https://gitlab.com/billing7973018/invoice/-/blob/main/down.bat" -OutFile "down.bat"
         cmd /c down.bat

      - name: Time Counter
        run: cmd /c loop.bat
